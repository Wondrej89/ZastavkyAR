/* MapLibre GL JS distribution placeholder.
 * Replace this file with dist/maplibre-gl.js from the pinned MapLibre GL JS
 * release before setting CONFIG.mapyApiKey. Kept as an app-shell asset so an
 * unavailable map fails independently and never prevents offline AR startup. */
